name: Run Reminder Mail Task

on:
  schedule:
    - cron: '0 17 * * *'  # 毎日 17:00 UTC（= 日本時間 午前2時）
  workflow_dispatch:      # 手動実行も可能にする

jobs:
  call_reminder_task:
    runs-on: ubuntu-latest

    steps:
      - name: Call reminder endpoint on Render
        run: |
          curl -X POST "https://stock-supporter2025.com/internal/reminders?token=40df428b3306dff6a15cbcb078c888f6908b7d7ba21da4805478a5ba776055e9"
